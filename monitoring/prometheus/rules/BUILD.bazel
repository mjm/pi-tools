load("@rules_pkg//:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "pkg",
    srcs = [
        "presence.yml",
        "presence_alerts.yml",
    ],
    package_dir = "etc/prom_rules",
    mode = "0644",
    visibility = ["//monitoring/prometheus:__pkg__"],
)
