service {
  name    = "prometheus"
  port    = 9090
  address = "10.0.2.104"

  meta = {
    metrics_path = "/metrics"
  }

  checks = [
    {
      name     = "prometheus is ready"
      http     = "http://10.0.2.104:9090/-/ready"
      interval = "30s"
      timeout  = "5s"
    }
  ]
}
