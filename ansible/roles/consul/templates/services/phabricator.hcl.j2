service {
  name    = "phabricator"
  port    = 80
  address = "10.0.2.111"

  checks = [
    {
      name     = "phabricator HTTP"
      http     = "http://10.0.2.111/status/"
      header   = {
        "Host" = ["code.home.mattmoriarity.com"]
      }
      interval = "30s"
      timeout  = "5s"
    }
  ]
}
